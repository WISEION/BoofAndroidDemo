name: Build Mysicgramm APK

on:
  push:
    branches:
      - "claude/**"
    paths:
      - "mysicgramm/**"
      - "settings.gradle"
      - "build.gradle"
      - "gradle/**"
      - ".github/workflows/mysicgramm-build.yml"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Grant execute permission for gradlew
        run: chmod +x ./gradlew

      - name: Build debug APK
        run: ./gradlew :mysicgramm:assembleDebug --stacktrace

      - name: Upload APK
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: mysicgramm-debug-apk
          path: mysicgramm/build/outputs/apk/debug/*.apk
          if-no-files-found: error
